const fs=require("fs");["client/src/telegramWebApp.js","client/src/TelegramScannerPage.js"].forEach(p=>{let b=fs.readFileSync(p);console.log(p,"first bytes:",b.slice(0,6).toString("hex"));if(b.length>=2&&( (b[0]===0xFF&&b[1]===0xFE) || (b[0]===0xFE&&b[1]===0xFF) )){let content = b.toString((b[0]===0xFF&&b[1]===0xFE)?"utf16le":"utf16be");console.log("UTF16 BOM detected, len=",content.length);fs.writeFileSync(p, Buffer.from(content,"utf8"));console.log("Wrote as pure UTF8 no BOM");return;}if(b.length>=3&&b[0]===0xEF&&b[1]===0xBB&&b[2]===0xBF){b=b.slice(3);fs.writeFileSync(p,b);console.log("UTF8 BOM removed");return;}console.log("No BOM");});
